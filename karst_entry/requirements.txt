# Karst Entry — dépendances Python (exécution dans QGIS)
#
# À ce jour, le plugin n'utilise QUE la bibliothèque standard Python et
# l'API PyQGIS fournie par QGIS. Aucune dépendance externe n'est requise.
#
# Si une dépendance tierce est ajoutée au plugin, listez-la ici (une par
# ligne) ; les scripts install_deps.bat / install_deps.sh l'installeront
# automatiquement dans le Python de QGIS.
#
# Exemple :
#   reportlab>=4.0
